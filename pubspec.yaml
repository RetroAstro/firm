name: firm
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # third party dependencies
  leancloud_storage: ^0.6.2
  leancloud_official_plugin: ^1.0.0-beta.12
  flutter_hooks: ^0.15.0
  provider: ^4.3.3
  flutter_verification_code: ^1.0.0+1
  photo_view: ^0.9.2
  flutter_svg: ^0.17.4
  flutter_slidable: ^0.5.4

  # local dependencies
  server:
    path: server/
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - config.json
    - assets/
  #   - images/a_dot_burr.jpeg

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
